[project]
name = "car-pipeline-scrapper"
version = "2026.09.03"
dependencies = [
   "psycopg[binary]",
   "playwright",
   "bs4",
   "lxml"
]
requires-python = ">=3.13.15"
authors = [
   {name = "Saul Torres Camas", email = "camas17st@outlook.com"}
]
description = "This is the scrapper pipeline's worker."

[tool.setuptools]
py-modules = ["scrapping", "scrapper"]